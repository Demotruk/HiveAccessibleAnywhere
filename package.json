{
  "name": "hive-accessible-anywhere",
  "version": "0.1.0",
  "private": true,
  "description": "Make HBD savings accessible from anywhere, including regions where Hive RPC nodes are blocked",
  "scripts": {
    "build:wallet": "cd wallet && npm run build",
    "build:invite": "cd invite && npm run build",
    "build:restore": "cd restore && npm run build",
    "dev:wallet": "cd wallet && npm run dev",
    "dev:invite": "cd invite && npm run dev",
    "dev:restore": "cd restore && npm run dev",
    "dev:proxy": "cd proxy && npm run dev",
    "build": "npm run build:wallet && npm run build:invite",
    "deploy:pages": "cd scripts && npm run deploy-pages && npm run generate-qr"
  }
}
